//@@license@@

#ifndef RESTERRORS
#define RESTERRORS

namespace ezechiel {
    enum class RestErrors {
        //! The user's password is needed but not given, see the -P global option.
    };
}
#endif // RESTERRORS

